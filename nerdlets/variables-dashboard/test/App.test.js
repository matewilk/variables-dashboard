import React from "react";
import { render, screen, waitFor } from "test-utils";
import { App } from "../App";

describe("App", () => {
  // beforeEach(() => waitFor(() => render(<App />)));

  it("renders hello text", () => {
    // expect(screen.getByText("Hello from App!")).toBeTruthy();
  });
});
